  <h1>Remove Category</h1>

  <p>Are you sure you want to <strong class="warning">permanently remove</strong> the following product?</p>
  
<% if @category.products.count > 0 then %>
  <p><strong class="warning">NOTE: This will also delete the <%= pluralize(@category.products.count, 'product') %> that are assigned to this Category.</strong></p>
<% end %>

  <table class="index" id="products" cellpadding="0" cellspacing="0" border="0">
    <tbody>
      <tr class="node level-1">
        <td class="product">
          <%= h @category.title %>
        </td>
      </tr>
    </tbody>
  </table>

  <% form_tag(admin_category_url(@category), :method => :delete) do %>
    <p class="buttons">
      <input class="button" type="submit" value="Delete Category"/>
      or
      <%= link_to 'Cancel', admin_products_url %>
    </p>
  <%- end -%>
