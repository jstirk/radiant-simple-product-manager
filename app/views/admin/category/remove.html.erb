  <h1>Remove Category</h1>

  <p>Are you sure you want to <strong class="warning">permanently remove</strong> the following category?</p>
  
<% if @category.products.count > 0  or @category.courses.count > 0 then %>
  <p><strong class="warning">NOTE: This will also delete any products or courses assigned to this Category.</strong></p>
<% end %>

  <table class="index" id="products" cellpadding="0" cellspacing="0" border="0">
    <tbody>
      <tr class="node level-1">
        <td class="product">
          <%= image('category/category', :alt => 'category-icon') %>
          <%= h @category.title %>
        </td>
      </tr>
    </tbody>
  </table>

  <% form_tag do %>
    <p class="buttons">
      <input class="button" type="submit" value="Delete Category"/>
      or
      <%= link_to 'Cancel', product_index_url %>
    </p>
  <%- end -%>
